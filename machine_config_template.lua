-- Machine Configuration Template
-- This file is used by the server manager to provision new machines
-- Placeholders will be replaced with actual values during provisioning

return {
    machineId = "%MACHINE_ID%",
    privateKey = %PRIVATE_KEY%,
    machineType = "%MACHINE_TYPE%",
    gatewayChannel = %GATEWAY_CHANNEL%,
    gatewayPublicKey = %GATEWAY_PUBLIC_KEY%
}
